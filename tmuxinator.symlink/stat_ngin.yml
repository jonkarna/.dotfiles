project_name: stat_ngin
project_root: ~/projects/stat_ngin
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - git pull
  - server:
      layout: main-vertical
      panes:
        - bundle && bundle exec rake RAILS_ENV=test meta_data:reset_data && RAILS_ENV=test rails s -p 3001
        - rails c
